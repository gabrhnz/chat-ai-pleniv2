# ============================================
# OpenAI / OpenRouter Configuration
# ============================================
OPENAI_API_KEY=sk-or-v1-your-openrouter-api-key-here
OPENAI_MODEL=openai/gpt-4o-mini
# For OpenRouter, use format: openai/gpt-4o-mini, anthropic/claude-3-sonnet, etc.

# OpenRouter specific (leave empty if using OpenAI directly)
USE_OPENROUTER=true
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_APP_NAME=FAQ-Bot-Universitario
OPENROUTER_SITE_URL=https://your-site.vercel.app

# Embeddings Model
EMBEDDING_MODEL=text-embedding-3-small
# Options: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002

# ============================================
# Supabase Configuration
# ============================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
# Service role key is required for admin operations (bypasses RLS)

# ============================================
# RAG Configuration
# ============================================
TOP_K_RESULTS=5
# Number of similar FAQs to retrieve
SIMILARITY_THRESHOLD=0.7
# Minimum similarity score (0-1) for search results
MAX_CONTEXT_LENGTH=3000
# Maximum characters for context sent to LLM

# ============================================
# Server Configuration
# ============================================
PORT=3000
NODE_ENV=development
# Options: development, production, test

# ============================================
# Rate Limiting
# ============================================
RATE_LIMIT_WINDOW_MS=900000
# 15 minutes in milliseconds
RATE_LIMIT_MAX_REQUESTS=100
# Max requests per window per IP

# ============================================
# Security
# ============================================
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-frontend.vercel.app
# Comma-separated list of allowed CORS origins

ADMIN_API_KEY=your-secret-admin-api-key-change-this
# API key for admin endpoints (/api/admin/*)

# ============================================
# Logging
# ============================================
LOG_LEVEL=info
# Options: error, warn, info, http, verbose, debug, silly

# ============================================
# OpenAI Request Configuration (optional overrides)
# ============================================
MAX_TOKENS=500
# Maximum tokens for completion
TEMPERATURE=0.3
# Creativity level (0-2, lower = more deterministic, recommended 0.3 for FAQs)

